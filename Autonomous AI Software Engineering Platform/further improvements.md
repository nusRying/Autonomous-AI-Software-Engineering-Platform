Infrastructure & DevOps Tools: The first document explicitly chose your task queues (Redis/RabbitMQ), project storage (MinIO/Git), and task schedulers (Airflow/Temporal). The new document leaves the infrastructure layer blank.

Observability Stack: The first document established a clear monitoring trio (Prometheus, Grafana, Loki) which is essential for tracking autonomous agents. The new document drops this entirely.

Security Protocols: The first blueprint outlined specific safety measures (Sandboxed code execution, container isolation, log auditing). The latest version mentions a "Test Environment Execution" but loses the strict security context.

User Roles & Permissions: The first document defined who can actually use the system (Administrator, Developer, Observer). The new document only mentions a generic "user."
